# English App

## Environment Variables

The app connects to the backend at `https://bot.mardeen.ir` by default.
To point to a different backend, edit `lib/core/constants.dart`:

```dart
const String baseUrl = 'https://bot.mardeen.ir';
```

No `.env` file is needed for the Flutter app itself. Environment configuration
is handled via the Dart source code constants.
